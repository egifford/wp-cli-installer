#!/usr/bin/env bash

UTILS_DIR=utils
BIN_DIR=bin
BIN_DIR_ABSOLUTE=bin # This needs to be set by the calling script.
VENDOR_DIR=vendor
VENDOR_DIR_ABSOLUTE=vendor # This needs to be set by the calling script.
